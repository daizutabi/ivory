library: tensorflow
datasets:
  data:
    class: mnist.Data
  dataset:
  fold: 0
model:
  call: mnist.create_model
results:
trainer:
  model: $
  optimizer: adam
  loss: sparse_categorical_crossentropy
  metrics: [accuracy]
  epochs: 5
