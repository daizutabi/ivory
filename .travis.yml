language: python
dist: xenial

matrix:
  include:
    - os: linux
      python: 3.7
    - os: linux
      python: 3.8

install:
  - pip install pycodestyle pyflakes mypy coveralls pytest pytest-cov
  - pip install -e .

before_script:
  - pycodestyle ivory
  - pyflakes ivory
  # - mypy pheasant
  - pycodestyle tests
  - pyflakes tests

script:
  - pytest

after_success:
  - coveralls
