# # Overview

# In this tutorial,  as a simple toy example, we try to predict `area` of rectangles
# which have `width` and `height`, but they include some noises.

# Using Ivory, we can write some codes like below:

# #File {%=/tests/example.py%} example.py

# Then define a learning process in on YAML file:

# #File {%=/tests/params.yaml%} params.yaml

# In the following sections, we will see in detail step by step.
